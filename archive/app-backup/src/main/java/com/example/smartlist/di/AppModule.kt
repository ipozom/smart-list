package com.example.smartlist.di

/**
 * Hilt DI module removed for the lightweight scaffold.
 *
 * This project includes a minimal ServiceLocator (see com.example.smartlist.ServiceLocator)
 * which builds the Room database and Repository at runtime without Hilt. If you later
 * re-enable Hilt, restore the original AppModule implementation here and re-add the
 * Hilt Gradle plugin in build.gradle.kts.
 */
object AppModule {
    // intentionally empty — Hilt module removed to avoid annotation-processor/plugin mismatches
}
